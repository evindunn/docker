display_name: Create Image
short_description: Generate a Docker image workflow and context
default_prompt: Create a new image in this repository from an image slug and build context, scaffolding the build context directory or Dockerfile if they are missing and generating the GitHub Actions workflow.
