display_name: Workflow Image README Sync
short_description: Sync README Docker image table from GitHub workflows
default_prompt: Scan .github/workflows for Docker images built by this repo and update README.md with the discovered images, base images, build contexts, workflow paths, and descriptions inferred from each build context.
