display_name: Workflow Image README Sync
short_description: Sync README Docker image table from GitHub workflows
default_prompt: Scan .github/workflows for Docker images built by this repo, read each build context, write concise human descriptions, and update README.md with the discovered images, base images, build contexts, workflow paths, and those descriptions.
